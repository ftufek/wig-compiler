/* Produced errors: 
SYMBOL_ALREADY_PRESENT(a)
SYMBOL_ALREADY_PRESENT(a)
NO_RETURN(a)
FUNCTION_RETURN_TYPE_DONT_MATCH(a)
*/
service {
  const html a = <html></html>;
  int a(bool a) { 
  }

  session a () { 
    return (1 + 1);
  }
}
