/* Produced errors: 
SYMBOL_ALREADY_PRESENT(a)
*/
service {
  const html a = <html></html>;
  session a () { 
    bool noreturn;
    if (true) { 
      noreturn = true;
      return 2;
    }
    else { 
      return 1;
    }
  }
}
